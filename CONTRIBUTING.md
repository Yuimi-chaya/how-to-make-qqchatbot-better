# 修订这本书

这本书优先解决实际私聊中的问题。欢迎纠正失效链接、版本差异、部署错误和表达不清的地方。

## 写作与核对

- 说明在什么版本和条件下观察到什么，不把一次成功或失败写成普遍机制。
- 把用户偏好和技术故障分开，不以自己的演绎口味统一所有人物。
- 修改一个建议时，同时检查前后章节是否仍然一致。
- 新增安装步骤要说明网络、数据目录和凭据边界，不默认读者已经看过前文。
- 引用原项目并尽量给出固定提交；上游经验文章与当前教程的取舍不同，应明确解释，不悄悄改写副本。
- 不提交真实会话、API Key、登录二维码或未获准分发的素材。

## 文章与配图

`articles/` 是经格式整理的来源副本，不是新教程的草稿区。更新来源时记录新提交、文章转换以及配图哈希；不要把封面混入正文图片。

图片使用相对于 Markdown 文件的路径，保留描述性替代文本。GitHub 不会把博客的 `/blog-assets/` 自动映射到本仓库，站点专用 HTML/CSS 也不应成为阅读依赖。

## 本地检查

需要 Python 3.10 或更新版本，无第三方依赖：

```sh
python scripts/validate_book.py
python -m unittest discover -s tests -v
```

检查覆盖本地链接、正文图片、文章和配图清单、UTF-8 与文件结构，不验证外部网站永远在线，也不等于浏览器渲染或真实部署测试。

涉及 Compose 时，可在已安装 Docker Compose 的环境执行：

```sh
docker compose --env-file .env -f examples/astrbot-napcat/compose.yaml config --quiet
```

其中 `.env` 必须是你准备好的测试环境配置。验证配置不需要登录 QQ，也不应自动拉起容器；真实安装按第 7 章执行。

新增效果主张需要相应证据。文档校验通过不能用来宣称“更拟人”或“某插件组合已经兼容”。
